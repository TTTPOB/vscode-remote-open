{
  "name": "remote-open",
  "displayName": "remote-open",
  "description": "Open mapped path on local from remote file tab",
  "version": "0.0.1",
  "publisher": "tttpob",
  "engines": {
    "vscode": "^1.103.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": ["onFileSystem:vscode-remote"],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "remote-open.copyMappedPath",
        "title": "Remote Open: Copy Local Mapped Path"
      },
      {
        "command": "remote-open.applyTransformer",
        "title": "Remote Open: Transform and Copy..."
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "when": "resourceScheme == 'vscode-remote'",
          "command": "remote-open.copyMappedPath",
          "group": "remote-open@1"
        },
        {
          "when": "resourceScheme == 'vscode-remote'",
          "command": "remote-open.applyTransformer",
          "group": "remote-open@2"
        }
      ]
    },
    "configuration": {
      "title": "Remote Open",
      "properties": {
        "remote-open.mappings": {
          "type": "array",
          "default": [],
          "description": "An array of remote to local path mappings.",
          "items": {
            "type": "object",
            "required": ["remote", "local"],
            "properties": {
              "remote": {
                "type": "string",
                "minLength": 1,
                "description": "Absolute POSIX path on the remote host."
              },
              "local": {
                "type": "string",
                "minLength": 1,
                "description": "Absolute client path prefix. Its slash style is preserved in copied paths."
              }
            }
          }
        },
        "remote-open.mappingFilePath": {
          "type": "string",
          "default": "",
          "description": "Path to a YAML file containing remote to local path mappings."
        },
        "remote-open.transformers": {
          "type": "array",
          "default": [],
          "description": "An array of transformers to apply to the mapped path.",
          "items": {
            "type": "object",
            "properties": {
              "name": {
                "type": "string",
                "description": "The name of the transformer, which will be shown in the context menu."
              },
              "rule": {
                "type": "string",
                "description": "The transformation rule. Use ${mapped} as a placeholder for the mapped path."
              }
            }
          }
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "pnpm run package",
    "compile": "pnpm run check-types && pnpm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "pnpm run check-types && pnpm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "pnpm run compile-tests && pnpm run compile && pnpm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.103.0",
    "@typescript-eslint/eslint-plugin": "^8.39.0",
    "@typescript-eslint/parser": "^8.39.0",
    "@vscode/test-cli": "^0.0.11",
    "@vscode/test-electron": "^2.5.2",
    "esbuild": "^0.25.8",
    "eslint": "^9.32.0",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.9.2",
    "@vscode/vsce": "^2.15.0"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  }
}
